// first import the array from java library

import util.java.*;   // it will import all the library
import util.java.ArrayList; // it will import only the ArrayList

Public class Array {

public static void main(String[] args) {
		
	ArrayList<String> vocation= new ArrayList<String>();  //t
	vocation.add("Paladin");
	vocation.add("Knight");
	vocation.add("Druid");
	vocation.add("Sorcare");
  
  }
  }
  
  
  Int Array[] = new Int[10];
