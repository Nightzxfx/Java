public class lessonOne {
	
	// shortcut, type "main" then ctrl + space
	
	public static void main(String[] args) {
		// comnent
		int age = 2;

		char letter = 'P';

		byte date = 10;
		String name = "Paulo";
		String lastName = "Valadares";
		//for each float you need add a F in the and and D for double
		
		float x = 4.6F;
		
		double y = 43.58394D;
		
	//shortcut, type "sysout" then ctrl +space
		
	//	System.out.println(age + "" + letter + date + " "+ (age*date) +" " + (date%age)+" " + (date/age)); 
	
		if (name.equals("Paulo")) 
		{
			System.out.println(date);
		}
		else 
		{
			System.out.println(letter);
		}
		
		if (lastName.equalsIgnoreCase("var32rladares") )
		{
		System.out.println("Yeah!!");	
		}
		else if (date > age) 
		{
		System.out.println("ALmost!");	
		}
		else
		{
		System.out.println("Nope dude!");
		}
		}
		
		}
